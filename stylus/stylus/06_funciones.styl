

/* Funciones */
sum(x, y)
	x + y
/* Función con parámetro opcional */
margintb(w, u = 3)
	w / u
section
	background-color #50d3de
	width sum(100px, 200px)
	margin margintb(@width) auto
	height @width